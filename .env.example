# Local development only. Copy to .env and use fresh random values.
ECOMMERCE_DB_PASSWORD=
ECOMMERCE_JWT_SECRET=
# Optional local admin account. Leave blank to disable bootstrap.
ECOMMERCE_ADMIN_EMAIL=
ECOMMERCE_ADMIN_PASSWORD=
